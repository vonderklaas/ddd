/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: ['@prisma/client'],
  experimental: {
    // Any experimental features go here
  },
  // Add any other Next.js configuration options below
};

module.exports = nextConfig; 